// ChainListener — reads the on-chain threat registry (Memo Program txs) and surfaces known payloads.
// TODO: implement listen(rpc_url: string): AsyncIterable<ThreatRecord>
export class ChainListener {}
