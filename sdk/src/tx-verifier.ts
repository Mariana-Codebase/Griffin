// TxVerifier — validates that a pending transaction matches the user's stated intent.
// Prevents attackers from redirecting transfers to attacker-controlled addresses.
// TODO: implement verify(intent: string, tx: object): { safe: boolean; reason?: string }
export class TxVerifier {}
